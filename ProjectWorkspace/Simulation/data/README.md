# Local Data Folder

This folder is intentionally kept light in Git.

The following artifacts are generated/downloaded locally and ignored:

- `esc50.zip`
- `esc50_raw/`
- `*.npy`

To rebuild locally:

```powershell
python models/ingest_esc50.py
```
